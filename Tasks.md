# Tasks — Đánh Giá Dinh Dưỡng Nhi Khoa

> File này theo dõi tiến độ thực tế của dự án. Cập nhật tick `[x]` ngay khi một mục hoàn thành và được kiểm chứng (chạy test/thử tay), không tick trước.

## Phase 1 — Chuyển đổi Full-Stack (React + Node + PostgreSQL + Docker)

- [x] **M1** — Scaffold monorepo (npm workspaces) + Docker dev environment, hot-reload xác nhận cho cả frontend (Vite) và backend (nodemon)
- [x] **M2** — Prisma schema (`Patient`) + migration đầu tiên, xác nhận qua `psql`
- [x] **M3** — Port engine tính toán (bảng WHO, năng lượng, vi chất, Z-score, thực đơn) sang service thuần TypeScript + 99 unit test
- [x] **M4** — API CRUD bệnh nhân (`/api/assessments`, `/api/patients`) + validate Zod + 10 integration test + smoke test tay qua HTTP
- [x] **M5** — Frontend React: design system port từ bản gốc, 3 tab (Nhập liệu / Kết quả / Nhật ký) nối API thật, React Query, 6 smoke test
- [x] **M6a** — CSV export server-side (`GET /api/patients/export/csv`), nút "Xuất CSV" ở tab Nhật Ký — đã xong từ M4, xác nhận qua integration test (content-type, escape dấu phẩy/ngoặc kép)
- [x] **M6b** — Port PDF export sang React (`PdfReportTemplate.tsx` + `PdfExportButton.tsx`, dùng lại `html2pdf.js`) — code hoàn chỉnh, type-check sạch, Vite load module không lỗi. ⚠️ **Chưa tự kiểm chứng được file PDF thực tế** (cần Canvas API của trình duyệt, không test headless được) — nhờ bạn bấm "Xuất PDF" ở tab Kết Quả để xác nhận file tải về đúng
- [x] **M7** — Production Docker build (multi-stage: builder+runtime cho backend, builder+Nginx cho frontend) + smoke test toàn luồng (tạo→xem→xuất CSV→xóa) qua Nginx proxy trên bản production thật

**🎉 Phase 1 (M1-M7) đã hoàn tất toàn bộ.** Ứng dụng chạy được cả ở môi trường dev (`docker-compose.dev.yml`, hot-reload) lẫn production (`docker-compose.prod.yml`, Nginx + build tối ưu).

## Phase 1.5 — Sửa chuẩn tăng trưởng WHO + module Import dữ liệu

- [x] Xác minh: Bộ Y tế Việt Nam (Quyết định 3777/QĐ-BYT-2024) **không có bảng riêng** — quy định dùng thẳng chuẩn WHO 2006 (0-5 tuổi) và WHO 2007 (5-19 tuổi). Số liệu Tổng điều tra Dinh dưỡng VN là thống kê hiện trạng dân số, không phải chuẩn để chấm điểm từng trẻ.
- [x] Lấy dữ liệu chính thức trực tiếp từ bộ dữ liệu WHO dùng trong phần mềm WHO Anthro/AnthroPlus (repo GitHub chính thức của WHO), xác minh khớp với vài mốc đã có sẵn trước khi thay
- [x] Sửa **cả 2 bảng** (`WFA`, `HFA`, cả nam và nữ) — phát hiện bảng nam ở đoạn >5 tuổi cũng sai (lệch tới ~3-10cm), không chỉ riêng bảng nữ như nghi ngờ ban đầu
- [x] Tách hẳn dữ liệu chuẩn tăng trưởng ra khỏi code: bảng `GrowthStandardPoint` trong Postgres + cache trong bộ nhớ, có dữ liệu WHO mặc định đi kèm (`growth-standards-who-default.ts`) làm nền nếu chưa import gì
- [x] Chức năng **Import CSV** — tab mới "📐 Chuẩn Tăng Trưởng": xem dữ liệu hiện tại, tải xuống CSV, nhập file CSV mới (thay thế toàn bộ, áp dụng ngay không cần khởi động lại). API: `GET/POST /api/growth-standards`, `/export`, `/import`
- [x] 18 test mới (12 unit + 6 integration) xác nhận: giá trị đúng, ranh giới WHO2006/2007, **và bé gái không còn cao hơn bé trai bất thường** (dữ liệu mới cho đúng mô hình dậy thì: gái vượt trai tạm thời 10-13 tuổi, khớp y văn)
- [x] Kiểm chứng qua HTTP thật trên cả dev và production: export/import/tính toán đều phản ánh đúng dữ liệu mới ngay lập tức

## Vấn đề đã phát hiện, chưa xử lý

- [ ] (Tối ưu, không khẩn) Bundle JS frontend production ~910KB (vượt cảnh báo 500KB của Vite) — nên code-split sau này, không ảnh hưởng đúng/sai chức năng
- [ ] Bảng `GrowthStandardPoint` chưa có giao diện xem trước dữ liệu import trước khi xác nhận (chỉ có confirm dialog cảnh báo) — nếu cần an toàn hơn có thể thêm bước "xem trước, so sánh với dữ liệu cũ" trước khi ghi đè

## Vấn đề đã phát hiện và đã xử lý xong (ghi lại để không lặp lại)

- [x] `docker-compose.dev.yml` và `docker-compose.prod.yml` cùng nằm trong thư mục `docker/` nên Docker Compose dùng chung 1 project name mặc định ("docker") — chạy prod đã **vô tình thay thế container dev đang chạy** cùng tên. Đã sửa bằng cách khai báo `name: dinhduong-dev` / `name: dinhduong-prod` riêng biệt ở đầu mỗi file. Dữ liệu không mất (nằm ở volume riêng `pgdata`/`pgdata_prod`) nhưng container bị thay — nếu gặp lại tình trạng dev "tự nhiên tắt", kiểm tra `docker ps -a` xem có bị đè project name không.
- [x] Dockerfile production của backend chỉ copy `backend/node_modules`, thiếu `node_modules` gốc (nơi npm workspaces hoist package `prisma` CLI) → lúc chạy `npx prisma migrate deploy` tự tải bản Prisma khác (7.8.0) từ mạng thay vì dùng bản đã khai báo (5.20.x). Đã sửa bằng cách copy cả 2 thư mục `node_modules` (gốc + backend) sang image runtime.
- [x] Thêm model mới vào `schema.prisma` (VD `Child`/`Guardian`) rồi chạy migration bên trong container backend dev **không** tự làm mới Prisma Client đang nằm trong bộ nhớ của tiến trình `nodemon` đang chạy — `prisma.<modelMới>` bị `undefined`, lỗi `TypeError: Cannot read properties of undefined (reading 'findMany')`, dù test tự động vẫn pass (dùng `PrismaClient` sinh riêng qua `testPrisma`). Phải chạy `docker compose exec backend npx prisma generate` **và** restart container sau mỗi lần đổi schema, không tự động theo `nodemon` watch.
- [x] Bấm nhanh nút "Gửi báo cáo" (`EmailReportButton.tsx`) 2-3 lần liên tiếp gửi ra 2-3 email/PDF riêng biệt, trong đó có bản trắng tinh — do `disabled={isSending}` chỉ chặn được sau khi React re-render (có độ trễ), một cú bấm nhanh (native double-click, hoặc do người dùng tưởng nút không phản hồi vì thiếu hẳn hiệu ứng loading so với "Xuất PDF") gọi `handleSend()` chồng lên nhau — cả 2 lệnh gọi dùng chung 1 khối HTML ẩn (`containerRef`) bật/tắt bằng `el.style.display` trực tiếp (không qua React), lệnh nào xong trước sẽ ẩn khối đó (`display:none`) đúng lúc lệnh còn lại đang giữa chừng chụp ảnh (html2canvas) → PDF ra trắng. Cùng lúc đó khối HTML ẩn (chứa nguyên trang báo cáo A4) là sibling của các nút trong cùng hàng flex — khi bật hiển thị để chụp, nó chèn thẳng vào giữa hàng nút gây giật hình rất khó chịu (nút "Xuất PDF" cũng có đúng lỗ hổng cấu trúc này nhưng được che bởi lớp phủ loading toàn màn hình sẵn có, còn "Gửi báo cáo" thì chưa từng có lớp phủ đó). Sửa bằng 1 chốt chặn **đồng bộ** qua `useRef` (không phụ thuộc React re-render) ở đầu `handleSend`/`handleExport` — áp dụng cho cả 2 nút vì chung lỗi; thêm lớp phủ loading toàn màn hình cho "Gửi báo cáo" giống "Xuất PDF" đã có. Xác minh bằng cách bấm 3 lần liên tiếp thật (native `button.click()` trong cùng 1 tick JS, không qua Playwright's actionability wait) → chỉ 1 email được gửi (trước đây sẽ ra 2-3), ảnh chụp giữa lúc gửi xác nhận lớp phủ che kín, không còn chèn giao diện.
- [x] Lỗi chèn giao diện ở trên **thật ra chưa được sửa tận gốc** ở bước trước (chỉ che bằng lớp phủ z-index) — khối HTML ẩn dùng làm nguồn chụp PDF (`#pdf-template`/`#pdf-template-email`, bọc `PdfReportTemplate`) vẫn là sibling trực tiếp của nút trong hàng flex ở `ResultTab.tsx`; lớp phủ `.pdf-generating` chỉ vẽ đè lên trên chứ không gỡ nó khỏi luồng bố cục — nếu overlay lỡ không kịp che (hoặc tắt CSS đó đi) thì khối A4 vẫn hiện y nguyên tại chỗ. Sửa triệt để bằng `React.createPortal` (lần đầu dùng trong dự án — đã rà soát không có convention off-screen CSS/portal nào có sẵn để tái dùng): tạo `frontend/src/pdf/pdfCaptureRoot.ts` — 1 `<div id="pdf-capture-root">` dùng chung cho cả 2 nút, tạo 1 lần lúc module load, gắn thẳng vào `document.body`, định vị `position:fixed; left:-9999px` (không dùng `display:none` cho khối host vì `html2canvas` cần layout thật để chụp — chỉ đặt lệch hẳn ra ngoài màn hình). Cả 2 khối template giờ portal vào đúng node dùng chung này thay vì nằm trong hàng nút — dù có bật `display:block` để chụp cũng không còn chạm tới giao diện hiển thị nữa, không còn phụ thuộc lớp phủ để che. Gộp luôn CSS `#pdf-template`/`#pdf-template-email` (trước đó `#pdf-template-email` không có rule CSS riêng, chỉ dựa vào `style` viết tay trong JSX — mong manh hơn) thành 1 class dùng chung `.pdf-capture-template`. Xác minh qua Playwright thật: `#pdf-capture-root` chỉ có 1, là con trực tiếp của `<body>`; cả 2 khối template không còn nằm trong hàng nút; **cố tình tắt CSS lớp phủ `.pdf-generating` để lộ lỗi cũ nếu còn** rồi bấm Xuất PDF — bounding box hàng nút giống hệt trước/trong lúc chụp, không còn giật hình; luồng xuất PDF và gửi email vẫn hoạt động bình thường.

## Tầng 2 — Tính năng Trung cấp (đang triển khai)

- [x] Biểu đồ tăng trưởng trực quan — 2 biểu đồ đường SVG tự viết (không thêm thư viện ngoài) trong tab "Chuẩn Tăng Trưởng": Cân nặng theo tuổi (WFA, 0–5 tuổi) và Chiều cao theo tuổi (HFA, 0–19 tuổi), tách riêng Nam/Nữ, có hover xem chi tiết từng mốc tháng tuổi. Đây là biểu đồ chuẩn tham chiếu WHO, chưa phải đường cong theo dõi nhiều lần khám của 1 bệnh nhân cụ thể (mục "Theo dõi xu hướng đa lần khám" bên dưới vẫn chưa làm).
- [x] Chuẩn kép WHO (≤5 tuổi) / WHO 2007 (>5 tuổi) — **thay hẳn công thức Z-score xấp xỉ cũ (`(value-median)/(median*hệ số cố định)`) bằng phương pháp LMS thật của WHO** (`Z = ((X/M)^L-1)/(L*S)`). Xác nhận: mục "hoặc CDC" trong đề bài là sót lại từ trước — Phase 1.5 đã xác nhận Việt Nam dùng thẳng WHO 2006/2007, không dùng CDC. Lấy trực tiếp cột L/S từ đúng các file nguồn WHO đã dùng ở Phase 1.5 (`weianthro.txt`/`lenanthro.txt`/`hfawho2007.txt`), cộng thêm 2 file mới `wflanthro.txt`/`wfhanthro.txt` cho chỉ số **Cân nặng/Chiều cao (WFH) — trước đây chưa từng có bảng WHO thật, chỉ tự chế từ WFA+HFA**. WFH giờ tra theo đúng chiều cao đo được (không theo tháng tuổi) vì bảng WHO gốc là vậy — lưu riêng dạng bundled data, không qua tab import CSV (khác trục dữ liệu với WFA/HFA). **Đổi định dạng CSV import/export** của tab "Chuẩn Tăng Trưởng" từ 5 cột thành 7 cột (`...,l,s,...`) — phá vỡ tương thích ngược có chủ đích, đã xác nhận với user. Phát hiện thêm 1 bug làm tròn ngày (Math.round thay vì Math.floor) gây sai lệch nhẹ ở các tháng 8/24/40/56. 25 test mới, xác minh qua HTTP thật khớp chính xác tính tay.
- [x] Cơ sở dữ liệu thành phần dinh dưỡng thực phẩm Việt Nam — `food-composition.data.ts` mới (kcal/carb/protein/fat thật cho 25 loại nguyên liệu), thay các hằng số mật độ tự chế trong `menu.service.ts` (carbDensity=0.28, fatFromMeat=wPro*0.05...). Năng lượng/macro mục tiêu vẫn tính độc lập theo tuổi/cân nặng như cũ (không đổi kiến trúc) — chỉ số gam nguyên liệu hiển thị giờ dựa trên dữ liệu thật. Tiện thể sửa 4 bug phát hiện được khi rà code: "bánh mì" bị nhận nhầm thành "bún/phở" (do check `.includes('mì')` chạy trước), "Sữa cao năng lượng" viết đầy đủ không khớp check "sữa cao nl", nhánh chết cho "sữa hạt" trong món chính (đã xóa), và món có "sữa" trong tên bị ép thành đồ ăn phụ dù là bữa chính có tinh bột. 9 test mới.
- [x] Lọc thực đơn theo dị ứng thực phẩm / tôn giáo / ăn chay — 7 nút chọn (chay, không hải sản/trứng/sữa/đậu phộng-hạt/thịt heo/thịt bò) ở tab Nhập Liệu. Món không hợp sẽ tự thay bằng món khác cùng bữa trong 7 món mẫu/tuần đã có sẵn (không cần bảng gắn thẻ thủ công cho 401 món — dùng nhận diện từ khóa như code có sẵn); nếu cả 7 món cùng bữa đều không hợp thì dùng món trung tính an toàn (cơm + đậu phụ + rau). Rà qua HTTP thật phát hiện thêm nhiều từ khóa thịt bị bỏ sót (lươn, bê, chả/nem/giò/pate = thịt chế biến, sốt vang = bò) — đã bổ sung. Còn sót 1-2 món tên phiên âm không có từ khóa rõ ràng (VD "Mì vằn thắn") — giới hạn đã biết của cách nhận diện từ khóa, không phải bug. 12 test mới.
- [x] Theo dõi xu hướng đa lần khám + Cảnh báo lâm sàng tự động — thêm hẳn bảng `Child` (hồ sơ trẻ với ID cố định, migration 2 giai đoạn: cột `Patient.childId` nullable → script `backfill-child-ids.mjs` gộp nhóm dữ liệu cũ theo tên+ngày sinh chuẩn hoá → bắt buộc NOT NULL) thay vì đoán tạm theo tên trùng khớp — trước đây mỗi lần khám là 1 dòng độc lập, không có cách nào biết 2 lần khám là cùng 1 trẻ. Tab Nhập Liệu có ô tìm kiếm hồ sơ trẻ đã có (gõ tên → gợi ý tên+ngày sinh+lần khám gần nhất từ `GET /api/children/search`, chọn 1 gợi ý tự điền và khoá vào đúng hồ sơ đó); không chọn gợi ý nào thì backend tự gộp theo tên+ngày sinh chuẩn hoá (`findOrCreateChild`). Tab Nhật Ký BN: cờ 🚩 tự động trên các dòng có cảnh báo (dùng lại danh sách đã tải sẵn, không gọi API thêm), nút "📈 Lịch sử" mở xem toàn bộ lịch sử 1 trẻ (`GET /api/children/:id/history`) kèm 3 biểu đồ xu hướng (cân nặng/chiều cao/BMI, SVG tự viết) và banner cảnh báo. Ngưỡng cảnh báo theo khuyến nghị WHO, tính bằng hàm thuần `computeGrowthAlerts` dùng chung frontend+backend (`packages/shared/src/growthAlerts.ts`): sụt cân bất kỳ mức nào giữa 2 lần khám liên tiếp, Z-score CN/Tuổi giảm ≥0.5 SD ("growth faltering"), chiều cao gần như không đổi (<0.1cm) sau >3 tháng (nguy cơ thấp còi) — có làm tròn 1 chữ số thập phân trước khi so sánh cân nặng/chiều cao để tránh báo giả do sai số float, và bỏ qua so sánh Z-score nếu 1 trong 2 lần khám không có (trẻ >60 tháng tuổi). 34 test mới (12 unit ngưỡng cảnh báo + 7 integration `/api/children` + bổ sung 4 case vào test tạo bệnh nhân có sẵn). Xác minh qua Docker + HTTP thật: 2 lần khám cùng tên (khác hoa/thường) tự gộp đúng 1 `childId`, chọn gợi ý có sẵn dù gõ tên khác vẫn gắn đúng hồ sơ, tạo 2 lần khám có sụt cân → cảnh báo hiện đúng qua endpoint lịch sử.
- [x] Thông tin Bố/Mẹ đầy đủ (bảng `Guardian`) — bắt buộc có người đại diện đủ điều kiện liên hệ — phát hiện qua rà soát: ban đầu chỉ có 1 trường `guardianEmail` duy nhất nằm trên từng lần khám (`Patient`), không có số điện thoại, và nếu bác sĩ quên gõ lại email ở lần khám có đặt "Ngày tái khám" thì `scanAndSendRevisitReminders()` không tìm thấy dù lần khám trước đã có — nhắc lịch bị mất âm thầm. Bước 1 đã chuyển liên hệ (email + SĐT mới) sang `Child` làm nguồn duy nhất; bước 2 (theo yêu cầu mở rộng của user) thay hẳn bằng bảng `Guardian` riêng — mỗi trẻ có tối đa 1 dòng "Bố" + 1 dòng "Mẹ" (`@@unique([childId, relationship])`, upsert theo cặp này), mỗi người đầy đủ Họ tên/Ngày sinh/Địa chỉ/Email/SĐT. Ràng buộc nghiệp vụ **bắt buộc ít nhất 1 người (Bố hoặc Mẹ) phải có cả email và SĐT** — kiểm ở tầng service (`hasQualifyingGuardian`), không phải constraint DB; áp dụng cả lúc tạo hồ sơ trẻ mới (chặn lưu nếu thiếu) lẫn lúc sửa sau này (chặn xoá liên hệ của người **duy nhất** đang đủ điều kiện, dùng Prisma transaction để rollback nếu vi phạm). Tab Nhập Liệu chỉ cần nhập nhanh 1 người đại diện (4 ô: Bố/Mẹ + Họ tên + Email + SĐT) — bắt buộc nếu trẻ chưa có ai đủ điều kiện, không bắt buộc lại nếu đã có sẵn; đầy đủ cả 2 người (kèm ngày sinh/địa chỉ) sửa sau ở panel Lịch Sử (tab Nhật Ký BN), mỗi người 1 thẻ riêng sửa độc lập qua `PUT /api/children/:id/guardians`. `scanAndSendRevisitReminders` giờ gửi email cho **tất cả** guardian có email của 1 trẻ (cả Bố lẫn Mẹ nếu cả 2 đều có), chuẩn bị sẵn cho tính năng gửi kết quả khám cho cả 2 phụ huynh sau này (chưa làm ở bước này). 15 test mới (6 unit `hasQualifyingGuardian` + 5 integration `PUT /guardians` + test tạo bệnh nhân mới thiếu người đại diện → 400 + test gửi email cho cả 2 guardian). Xác minh qua HTTP thật: tạo hồ sơ không kèm người đại diện → 400 đúng thông báo; tạo kèm đủ → 201; thêm Bố sau qua panel Lịch Sử → cả 2 người tồn tại độc lập, không đè lên nhau.
- [x] Quản lý người dùng & phân quyền (bác sĩ / điều dưỡng / admin) — JWT trong httpOnly cookie (không session store, sống sót qua restart nodemon), 3 vai trò với ma trận quyền: admin quản lý user + mọi thứ, bác sĩ toàn quyền lâm sàng trừ quản lý user/chuẩn tăng trưởng, điều dưỡng thêm/xem bệnh nhân nhưng không xóa. Tài khoản admin đầu tiên tự tạo từ `ADMIN_EMAIL`/`ADMIN_PASSWORD` trong `.env` lúc server khởi động nếu bảng User rỗng. 22 test mới (integration, 2 file mới `auth`/`users` + bổ sung vào 2 file cũ) xác nhận phân quyền, guard admin-cuối-cùng, và tự-vô-hiệu-hóa. Xác minh qua HTTP thật: login/me/logout, 401 khi chưa đăng nhập, 403 đúng vai trò.
- [x] Nhắc lịch tái khám qua email (SMS/Zalo chưa làm — cần tài khoản dịch vụ trả phí user chưa có) — trường "Email phụ huynh" mới (tùy chọn) trong form nhập liệu; job `node-cron` quét mỗi ngày (mặc định 8:00 sáng, cấu hình qua `REVISIT_REMINDER_CRON`) tìm bệnh nhân có ngày tái khám trong `REVISIT_REMINDER_DAYS_BEFORE` ngày tới (mặc định 3) và gửi email qua `nodemailer`, đánh dấu `revisitReminderSentAt` để không gửi trùng. **Dry-run mặc định**: chưa cấu hình `SMTP_*` trong `.env` thì chỉ log ra console thay vì gửi thật — điền SMTP thật vào sau không cần sửa code/build lại. 7 test mới (unit email dry-run/thật + integration quét đúng/sai điều kiện). Xác minh qua HTTP thật + gọi thẳng hàm quét trong container: tạo bệnh nhân có lịch tái khám gần + email → quét thấy đúng 1 người, đánh dấu đã gửi, quét lại lần 2 = 0 (không gửi trùng).
- [x] Tìm kiếm, lọc, thống kê danh sách bệnh nhân — tab Nhật Ký giờ có: tìm theo tên, lọc theo giới/tình trạng CN-CC (SDD cấp nặng/Suy dinh dưỡng cấp/Bình thường/Thừa cân/Béo phì)/khoảng ngày khám, và 4 ô thống kê (tổng số, % SDD, % thừa cân-béo phì, % bình thường) tự tính theo đúng danh sách đang lọc — hoàn toàn phía frontend (danh sách bệnh nhân vốn đã tải hết về máy), không cần API mới. Tiện thể ẩn luôn nút xóa hồ sơ với tài khoản Điều dưỡng (trước đó nút vẫn hiện nhưng bấm sẽ bị 403 — lỗ hổng UX sót lại từ lúc làm phân quyền). 2 test mới xác nhận lọc đúng theo tên và ẩn nút xóa theo vai trò.
- [x] Danh sách thực phẩm (CRUD) cho bác sĩ dinh dưỡng — tab mới "🍎 Danh Sách Thực Phẩm": bảng `Food` trong Postgres với đầy đủ công dụng/tác dụng, năng lượng (kcal + đạm/tinh bột/béo mỗi 100g), lưu ý theo bệnh lý (checkbox từ danh sách cố định 13 bệnh lý dùng chung FE/BE — `Tiểu đường/Suy thận/Gout/...`, cộng 1 ô ghi chú tự do), nguồn tham khảo. Form thêm/sửa/xoá từng món (`admin`+`bac_si` sửa được, các vai trò khác chỉ xem) + CSV import/export hàng loạt — import là **upsert theo tên**, khác hẳn kiểu "thay thế toàn bộ" của tab Chuẩn Tăng Trưởng, để không xoá mất dữ liệu hệ thống. **Tích hợp thẳng vào thực đơn tuần**: `menu.service.ts` giờ tra cứu dinh dưỡng nguyên liệu từ bảng `Food` (qua cache trong bộ nhớ, nạp lại ngay sau mỗi lần sửa — không cần khởi động lại server) thay vì file tĩnh `food-composition.data.ts` cũ; ~20 mục dùng để sinh thực đơn được đánh dấu `isSystemDefault` — sửa số liệu được nhưng không xoá được qua UI/API (chặn ở tầng service, trả lỗi 400) để không phá vỡ tính năng thực đơn đang chạy. 22 test mới (3 unit cho cache/fallback + 19 integration: CRUD, phân quyền, chặn xoá system-default, CSV import không xoá dữ liệu cũ). Xác minh qua Docker thật: migration áp dụng thành công, server tự seed đúng 20 thực phẩm lõi lúc khởi động, toàn bộ 191 test (bao gồm test thực đơn cũ không đổi) pass, type-check sạch cả 2 phía, và gọi API thật (login → list → tạo → xoá bị chặn ở system-default) khớp kỳ vọng. **Ghi chú môi trường phát sinh khi kiểm thử (không liên quan tính năng này)**: file `.env` của máy dev thiếu hẳn `JWT_SECRET`/`ADMIN_EMAIL`/`ADMIN_PASSWORD` nên trước đây không đăng nhập được — đã bổ sung giá trị tạm (`admin@dinhduong.local` / `DevAdmin123!`) để kiểm thử, nên đổi lại nếu cần.
- [x] Gửi báo cáo PDF qua email tại màn Kết Quả — nút "📧 Gửi báo cáo" cạnh "Xuất PDF"/"Lưu hồ sơ", gửi đúng file PDF (cùng nội dung với Xuất PDF) tới tất cả người đại diện (`Guardian`) có email của trẻ đó. PDF vẫn sinh ở client (`html2pdf.js`, giữ nguyên quyết định kiến trúc không dùng Puppeteer/Chromium server-side) — thêm đường dẫn xuất ra `Blob` (`outputPdf('blob')`) cạnh đường dẫn tải file `.save()` cũ, mã hoá base64 theo từng đoạn 32KB (tránh tràn stack với PDF nhiều trang) rồi gửi lên `POST /api/patients/:id/send-report` dạng JSON (không dùng multipart/multer — nâng giới hạn `express.json()` từ 100kb mặc định lên 15mb thay vì thêm dependency mới). Nút **vô hiệu khi đang xem bản chưa lưu** (xem trước từ tab Nhập Liệu, chưa có `patientId`) — quyết định đã chốt với user, tránh gửi nhầm dữ liệu chưa lưu. Tiện sửa luôn 1 lỗ hổng liên quan phát hiện khi nối dây: lưu trực tiếp từ tab Kết Quả (khác với lưu từ tab Nhập Liệu) trước đây không mang theo `childId`/thông tin người đại diện nên sẽ 400 với bệnh nhân hoàn toàn mới — thêm `currentAssessmentInput` vào `AppStateContext` để tab Kết Quả tái dùng đúng payload gốc. Cấu hình SMTP Gmail thật của phòng khám (`smtp.gmail.com:465`, mật khẩu ứng dụng) đã điền vào `.env` (gitignored, không lộ git), dùng chung với tính năng nhắc lịch tái khám có sẵn — không tạo biến môi trường song song. 7 test mới (2 unit `email.service` cho attachments + 5 integration `/send-report`: gửi thành công nhiều người, không ai có email → 400, id không tồn tại → 404). Xác minh qua HTTP thật với SMTP Gmail thật: tạo bệnh nhân → gửi báo cáo → nhận được email thật kèm PDF đính kèm tại hộp thư cấu hình (log không còn "[email dry-run]" khi SMTP đã cấu hình). **Phát hiện phụ trong lúc kiểm thử (không phải bug của tính năng này)**: sau khi thêm bảng `Child`/`Guardian` ở bước trước, container backend dev chưa từng chạy lại `prisma generate` kể từ đó nên Prisma Client đang chạy trong bộ nhớ bị thiếu hẳn 2 model này (`prisma.child`/`prisma.guardian` là `undefined`) dù test tự động vẫn pass (dùng `testPrisma` sinh riêng) — sửa bằng `docker compose exec backend npx prisma generate` + khởi động lại container. Ghi lại vì có thể tái diễn: đổi schema Prisma trong lúc dev container đang chạy cần `prisma generate` + restart, không tự động.
- [x] Xem lại lịch sử báo cáo đã gửi cho phụ huynh — bảng `ReportEmailLog` mới, mỗi lần gửi báo cáo (mỗi người nhận riêng, kể cả khi gửi cho cả Bố lẫn Mẹ) ghi 1 dòng: lần khám nào, gửi tới ai (snapshot tên+email tại thời điểm gửi — không đọc lại từ `Guardian` vì thông tin liên hệ có thể đổi sau), thành công hay thất bại (kèm `errorMessage` nếu lỗi), lúc nào. Ghi trực tiếp trong vòng lặp gửi email có sẵn ở `report.service.ts` (mỗi guardian 1 try/catch riêng, ghi log dù thành công hay thất bại, không thay đổi luồng gửi). Hiển thị ở đúng nơi tự nhiên nhất — panel "Lịch Sử" sẵn có (tab Nhật Ký BN → Lịch sử, cùng chỗ xem biểu đồ xu hướng/thông tin Bố Mẹ) — thêm 1 bảng "📧 Báo Cáo Đã Gửi" liệt kê toàn bộ lịch sử gửi của trẻ đó qua mọi lần khám, không cần API/route riêng vì tận dụng thẳng `GET /api/children/:id/history` đã có (đỡ 1 round-trip). Không làm tracking mở email/click-through (cần dịch vụ ngoài, vượt nhu cầu phòng khám nội bộ) và không lưu lại nội dung PDF đã gửi (sinh lại y hệt bất kỳ lúc nào từ dữ liệu, không cần tốn chỗ lưu). 2 test integration mới (ghi log khi gửi thành công + khi gửi thất bại, xác nhận cả 2 hiện đúng qua endpoint lịch sử) — tổng 239/239 test pass. Xác minh qua trình duyệt thật (Playwright, không chỉ curl): gửi báo cáo thật cho bệnh nhân "Phạm Thị Hồng Ngọc" → mở lại panel Lịch Sử → thấy đúng 1 dòng "✅ Đã gửi" với đúng tên/email người nhận và thời gian gửi.
- [ ] Báo cáo thống kê phòng khám (tỷ lệ SDD/thừa cân theo tháng, hiệu quả can thiệp)
- [ ] Backup dữ liệu định kỳ, audit trail thao tác người dùng

## Tầng 3 — Tính năng Nâng cao (tầm nhìn dài hạn)

- [ ] Tích hợp HIS/EMR/LIS bệnh viện (chuẩn HL7/FHIR)
- [ ] AI dự đoán nguy cơ SDD/béo phì dựa trên xu hướng tăng trưởng
- [ ] App di động cho phụ huynh (nhật ký ăn uống tại nhà, nhắc nhở, xem lịch sử khám)
- [ ] Nhận diện ảnh chụp thức ăn để ước tính khẩu phần thực tế
- [ ] Tự động tối ưu thực đơn (thay thực đơn mẫu cố định bằng thuật toán cân bằng dinh dưỡng/chi phí/khẩu vị)
- [ ] Telemedicine / tư vấn dinh dưỡng từ xa
- [ ] Chữ ký số bác sĩ trên phiếu kết quả (giá trị pháp lý hồ sơ bệnh án điện tử)
- [ ] Multi-tenant SaaS cho chuỗi phòng khám
- [ ] Tuân thủ Nghị định 13/2023/NĐ-CP về bảo vệ dữ liệu cá nhân (đặc biệt nhạy cảm vì là dữ liệu trẻ em + sức khoẻ)
